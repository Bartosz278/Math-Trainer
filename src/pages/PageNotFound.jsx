import Spinner from "../components/Spinner";

function PageNotFound() {
  return (
    <div>
      Page doesn't exist
      <div>
        <Spinner />
      </div>
    </div>
  );
}

export default PageNotFound;
